node_modules/
dist/
coverage/
*.log
.DS_Store
*.tsbuildinfo
.vite/
# CHANGELOG.md is auto-copied from repo root by the `prepack` script (and
# removed by `postpack`). Any copy left behind locally is transient — never
# commit it.
CHANGELOG.md

# Generated by `npm run docs:check` (scripts/check-readme.mjs). Removed on
# success; preserved on failure so the developer can re-run tsc manually.
.docs-check/
